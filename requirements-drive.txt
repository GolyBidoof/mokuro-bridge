# Optional Google Drive upload provider.
# Install only if you want to upload finished volumes to Google Drive:
#   pip install -r requirements-drive.txt
# Then authenticate once: python server.py --setup-upload drive
# The wizard walks you through creating a free Google Cloud "Desktop app"
# OAuth client and pasting its Client ID (PKCE flow — no client_secret.json
# needed). Set DRIVE_CLIENT_ID to preset the client ID, or
# DRIVE_CLIENT_SECRET_FILE for a full client_secrets.json, or save a
# service-account JSON as the drive credentials file.
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0
google-auth>=2.0.0
requests>=2.20.0
