# ── Secrets / personal data (never commit!) ─────────────────────────────
# Session cookies / credential files
cookies.json
yahoo_cookies.json
.env
*.env
!.env.example

# ── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# ── Node ─────────────────────────────────────────────────────────────────
node_modules/

# ── macOS ────────────────────────────────────────────────────────────────
.DS_Store

# ── Misc local artifacts ─────────────────────────────────────────────────
_debug*
_preview*
*.log
manga_archives/
manga_pages/
manga_archive/
manga_archive.zip

# Bridge output / local OCR work (see MOKURO_BRIDGE_OUTPUT_DIR / WORK_DIR)
/output/

# Custom mokuro fork checkout (kept local; use vanilla pip mokuro or point
# MOKURO_REPO at your own fork elsewhere)
/mokuro/

# Local browser userscripts (kept out of the public repo)
/*.user.js
