# ── Server ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
# Page-fetch accelerator: serves the CDN proxy on extra localhost ports
# (mokuro_bridge/fetchproxy.py). Usually already present via FastAPI's
# ecosystem, but relied on directly now.
httpx>=0.27.0

# ── OS credential store (optional but recommended) ─────────────────────
# Enables storing MEGA credentials in the system keychain / credential
# store (macOS Keychain, Windows Credential Manager, Linux Secret Service).
# Without it, credentials are kept in a plain 0600 file instead.
keyring>=23.13

# ── OCR engine: NOT installed here ─────────────────────────────────────
# mokuro pulls in PyTorch, which is several GB, and the bridge runs fine
# without it: page capture, the fetch accelerator and every upload provider
# work, and /health reports mokuro_installed:false. OCR needs an engine, so
# install one when you want it:
#
#   pip install -r requirements-ocr.txt     # stock mokuro from PyPI
#   export MOKURO_REPO=/path/to/checkout    # or a faster fork checkout
